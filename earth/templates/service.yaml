apiVersion: v1
kind: Service
metadata:
  name: {{ include "earth.fullname" . }}
  labels:
    {{- include "earth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "earth.selectorLabels" . | nindent 4 }}
