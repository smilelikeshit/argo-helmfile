apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "earth.fullname" . }}-test-connection"
  labels:
    {{- include "earth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "earth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
